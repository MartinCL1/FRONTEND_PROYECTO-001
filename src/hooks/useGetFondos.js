import { useEffect } from 'react'

const getFondos = (url) => {
    useEffect(() => {
    }, [])

    // Estas funciones deben de ser tratadas en services.
    const obtenerImagenes = async () => {
        try {
            const urlImagenes =  await fetch('http://localhost:3000/')
        } catch {

        }
    }
}
